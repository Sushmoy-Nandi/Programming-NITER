#include <stdio.h>
int main() 
{
    int inputA,inputB,output;
    scanf("%d %d",&inputA,&inputB);

    //NAND

    // output=!(inputA && inputB);
    // printf("NAND output: %d\n",output);

    //NOR
    // output=!(inputA || inputB);
    // printf("NOR output: %d\n",output);

    //NOT
    // output=!inputA;
    // printf("NOT output: %d\n",output);

    return 0;
}
